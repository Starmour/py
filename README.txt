# Импорт библиотеки для работы с математическими функциями
# Пустой список, для сохранения извлеченных из файла данные
# Путь к файлу
# Флаг "читаемости", который включается после начала таблицы
# Безопасное открытие файла
# Построчнй перебор файла
# Удаление пробеов по краям текущей строки 
# Начало чтения после заголовка таблицы
# Если чтрока начинается с "ID"
# то флаг переключается в True
# переход на новую строку
# Остановка чтения, если встретили пустую строку или текст вне таблицы
# если флаг включен и строка пустая
# то прервать цикл
# При включении флага
# Разделяем строку по пробельным символам на ряды
# Если количество рядов в строке больше либо равно 4,
# то формируем словарь из ключей и значений
# и добавляем словарь в список словарей
# построчный вывод значений каждой записи сллваря
# Объект для предсказания с входными данными
# добавляем в словарь item списка data новый ключ dist со значением равным эвклидовому растоянию от точки выборки до точки объекта
# сортировка сравнение словарей в списке data по значеию ключа dist при помощи лямбда функции
# отбор значений в отсортированном списке от 0 до k (не включая)
# Подсчет голосов за классы
# создание словаря голосов
# пробегаем по отсортированному списку, где n - это словарь с ключами и значениями
# переменной cls присваиваем значение с ключом Class в словаре n
# запись в словарь голосов количества ключей равных значениям переменной cls (если таких нет - 0, и запись в 
# сравнение значений, полученных по ключам словаря votes
